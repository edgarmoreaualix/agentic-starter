# QA checklist

- Open `frontend/apps/orch-06-webapp/` with a static server.
- Confirm title renders from `project_id`.
- Confirm chips show `pink`, `#eb2f96`, and `#fff0f6`.
- Confirm backend `status` and each feature item are visible.
- Confirm specialist block renders file text without truncation.
- Simulate missing API path and confirm visible error text.
