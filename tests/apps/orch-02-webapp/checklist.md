# QA checklist: orch-02-webapp

- Open the app in a browser and verify the title renders from backend `project_id`.
- Confirm three color chips are visible: `blue`, `#1677ff`, `#f0f5ff`.
- Confirm status renders as `active` and features appear as a list.
- Confirm specialist block displays exact text from `specialist.txt`.
- Temporarily break one fetch path and confirm error handling message appears.
