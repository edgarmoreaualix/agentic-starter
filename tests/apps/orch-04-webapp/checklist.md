# QA Checklist

- Confirm page loads from `frontend/apps/orch-04-webapp/index.html` via static server.
- Confirm title renders `orch-04-webapp`.
- Confirm color chips include `orange`, `#fa8c16`, and `#fff7e6`.
- Confirm backend status displays as `active`.
- Confirm features list renders all backend items.
- Confirm specialist block renders exact `specialist.txt` content.
- Confirm no console fetch errors in browser devtools.
