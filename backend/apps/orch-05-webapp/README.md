# orch-05-webapp backend payload

This directory provides the runtime JSON consumed by the frontend page.

- `api.json` is fetched by the UI from `/backend/apps/orch-05-webapp/api.json`.
- `meta.json` stores ownership and loop/version metadata for traceability.

The frontend uses this payload to render project identity, colors, status, and feature chips.
